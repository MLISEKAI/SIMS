@model IEnumerable<SIMS.Students>

@{
    ViewData["Title"] = "Index";
}

<h1>Student Info</h1>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Date of Birth</th>
            <th>Address</th>
            <th>Phone</th>
            <th>Email</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Student_ID</td>
                <td>@item.Student_Name</td>
                <td>@item.DateOfBirth.ToShortDateString()</td>
                <td>@item.Address</td>
                <td>@item.Phone</td>
                <td>@item.Email</td>
                <td>
                    <a asp-action="EditSt" asp-route-id="@item.Student_ID">Edit</a> |
                    <a asp-action="DetailsSt" asp-route-id="@item.Student_ID">Details</a> |
                    <a asp-action="DeleteSt" asp-route-id="@item.Student_ID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<form asp-controller="Admin" asp-action="Home" method="post" class="form-inline">
    <button type="submit" class="btn bg-primary">Back to home</button>
</form>
 
